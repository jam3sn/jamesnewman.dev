---
title: Blog
layout: base
---
<section>
    <h1>Blog</h1>
    <h4>My collection of technical rambles</h4>
</section>

<section>
    {%- for post in collections.post -%}
    <article>
        <a href="{{ post.url }}" alt="{{ post.data.title }}">
            <h2>{{ post.data.title }}</h2>
        </a>

        <p>{{ post.data.summary }}</p>
    </article>
    {%- endfor -%}
</section>